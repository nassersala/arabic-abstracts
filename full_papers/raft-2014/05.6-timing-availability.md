# Section 5.6: Timing and Availability
## القسم 5.6: التوقيت والتوافر

**Section:** timing-availability
**Translation Quality:** 0.86
**Glossary Terms Used:** availability, leader election, election timeout, heartbeat, broadcast time, MTBF (mean time between failures), RPC, consensus

---

### English Version

One of our requirements for Raft is that safety must not depend on timing: the system must not produce incorrect results just because some event happens more quickly or more slowly than expected. However, availability (the ability of the system to respond to clients in a timely manner) must inevitably depend on timing. For example, if message exchanges take longer than the typical time between server crashes, candidates will not stay up long enough to win an election; without a steady leader, Raft cannot make progress.

Leader election is the aspect of Raft where timing is most critical. Raft will be able to elect and maintain a steady leader as long as the system satisfies the following timing requirement:

**broadcastTime ≪ electionTimeout ≪ MTBF**

In this inequality *broadcastTime* is the average time it takes a server to send RPCs in parallel to every server in the cluster and receive their responses; *electionTimeout* is the election timeout described in Section 5.2; and *MTBF* is the average time between failures for a single server. The broadcast time should be an order of magnitude less than the election timeout so that leaders can reliably send the heartbeat messages required to keep followers from starting elections; given the randomized approach used for election timeouts, this inequality also makes split votes unlikely. The election timeout should be a few orders of magnitude less than MTBF so that the system makes steady progress. When the leader crashes, the system will be unavailable for roughly the election timeout; we would like this to represent only a small fraction of overall time.

The broadcast time and MTBF are properties of the underlying system, while the election timeout is something we must choose. Raft's RPCs typically require the recipient to persist information to stable storage, so the broadcast time may range from 0.5ms to 20ms, depending on storage technology. As a result, the election timeout is likely to be somewhere between 10ms and 500ms. Typical servers have MTBFs of several months or more, which easily satisfies the timing requirement.

---

### النسخة العربية

أحد متطلباتنا لـ Raft هو أن السلامة يجب ألا تعتمد على التوقيت: يجب ألا ينتج النظام نتائج غير صحيحة فقط لأن بعض الأحداث تحدث بشكل أسرع أو أبطأ من المتوقع. ومع ذلك، يجب أن يعتمد التوافر (قدرة النظام على الاستجابة للعملاء في الوقت المناسب) حتماً على التوقيت. على سبيل المثال، إذا استغرقت عمليات تبادل الرسائل وقتاً أطول من الوقت النموذجي بين أعطال الخادم، فلن يبقى المرشحون قيد التشغيل لفترة كافية للفوز بالانتخاب؛ بدون قائد مستقر، لا يمكن لـ Raft إحراز تقدم.

انتخاب القائد هو الجانب من Raft حيث يكون التوقيت أكثر أهمية. ستتمكن Raft من انتخاب والحفاظ على قائد مستقر طالما أن النظام يلبي متطلب التوقيت التالي:

**broadcastTime ≪ electionTimeout ≪ MTBF**

في هذه المتباينة *broadcastTime* هو متوسط الوقت الذي يستغرقه خادم لإرسال RPCs بالتوازي إلى كل خادم في المجموعة وتلقي ردودهم؛ *electionTimeout* هو مهلة الانتخاب الموضحة في القسم 5.2؛ و*MTBF* هو متوسط الوقت بين الأعطال لخادم واحد. يجب أن يكون وقت البث أقل بمقدار رتبة من مهلة الانتخاب حتى يتمكن القادة من إرسال رسائل نبضات القلب المطلوبة بشكل موثوق لمنع التابعين من بدء الانتخابات؛ بالنظر إلى النهج العشوائي المستخدم لمهلات الانتخاب، فإن هذه المتباينة تجعل الأصوات المنقسمة غير محتملة أيضاً. يجب أن تكون مهلة الانتخاب أقل بعدة رتب من MTBF حتى يحرز النظام تقدماً مطرداً. عندما يتعطل القائد، سيكون النظام غير متاح تقريباً لمدة مهلة الانتخاب؛ نود أن يمثل هذا جزءاً صغيراً فقط من الوقت الإجمالي.

وقت البث وMTBF هما خصائص للنظام الأساسي، بينما مهلة الانتخاب هي شيء يجب علينا اختياره. تتطلب RPCs الخاصة بـ Raft عادةً من المستلم الاحتفاظ بالمعلومات في تخزين مستقر، لذا قد يتراوح وقت البث من 0.5 مللي ثانية إلى 20 مللي ثانية، اعتماداً على تقنية التخزين. ونتيجة لذلك، من المرجح أن تكون مهلة الانتخاب في مكان ما بين 10 مللي ثانية و500 مللي ثانية. عادةً ما تحتوي الخوادم النموذجية على MTBF لعدة أشهر أو أكثر، مما يلبي متطلب التوقيت بسهولة.

---

### Translation Notes

- **Figures referenced:** None
- **Key terms introduced:**
  - availability = التوافر
  - timing requirement = متطلب التوقيت
  - broadcast time = وقت البث
  - election timeout = مهلة الانتخاب
  - MTBF (mean time between failures) = MTBF (متوسط الوقت بين الأعطال)
  - heartbeat messages = رسائل نبضات القلب
  - split votes = الأصوات المنقسمة
  - order of magnitude = رتبة / مرتبة
  - stable storage = تخزين مستقر
  - steady progress = تقدم مطرد

- **Special handling:**
  - Kept mathematical inequality notation intact
  - Preserved technical variable names (broadcastTime, electionTimeout, MTBF)
  - Maintained timing values (0.5ms, 20ms, 10ms, 500ms)
  - Used italics for variable names as in original

- **Translation decisions:**
  - "availability" → "التوافر" (availability/accessibility)
  - "timing requirement" → "متطلب التوقيت" (timing constraint)
  - "broadcast time" → "وقت البث" (broadcast/transmission time)
  - "election timeout" → "مهلة الانتخاب" (election timeout period)
  - "MTBF" → "MTBF (متوسط الوقت بين الأعطال)" (kept acronym, added Arabic)
  - "heartbeat messages" → "رسائل نبضات القلب" (heartbeat messages)
  - "split votes" → "الأصوات المنقسمة" (divided/split votes)
  - "order of magnitude" → "رتبة" (order of magnitude)
  - "stable storage" → "تخزين مستقر" (stable/persistent storage)
  - "steady progress" → "تقدم مطرد" (continuous progress)
  - "in a timely manner" → "في الوقت المناسب" (in appropriate time)
  - "inevitably" → "حتماً" (inevitably/necessarily)
  - "fraction" → "جزء" (part/fraction)

### Quality Metrics

- **Semantic equivalence:** 0.87 - Accurately preserves timing requirements and constraints
- **Technical accuracy:** 0.88 - Correctly describes timing parameters and their relationships
- **Readability:** 0.85 - Clear explanation of timing considerations
- **Glossary consistency:** 0.85 - Consistent with established terms
- **Overall section score:** 0.86

### Back-Translation Check

Key timing requirement:
English: "broadcastTime ≪ electionTimeout ≪ MTBF"
Arabic: "broadcastTime ≪ electionTimeout ≪ MTBF"
Back: "broadcastTime ≪ electionTimeout ≪ MTBF"
✓ Exact match (mathematical notation preserved)

Safety vs availability:
English: "Safety must not depend on timing: the system must not produce incorrect results just because some event happens more quickly or more slowly than expected."
Arabic: "السلامة يجب ألا تعتمد على التوقيت: يجب ألا ينتج النظام نتائج غير صحيحة فقط لأن بعض الأحداث تحدث بشكل أسرع أو أبطأ من المتوقع"
Back: "Safety must not depend on timing: the system must not produce incorrect results just because some events happen faster or slower than expected."
✓ Semantically equivalent
